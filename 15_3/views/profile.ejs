<!DOCTYPE html>
<html lang="en">
  <%- include ('./partials/header.ejs') %>
  <body>
    <div class="container card text-center mt-2">
      <div class="card-header">Product</div>
      <div class="card-body center">
        <div class="row">
          <div class="col-md-3 mx-auto">
            <h1 class="card-title"><%= profile['name']%></h1>
            <p class="card-text">
              With supporting text below as a natural lead-in to additional
              content.
            </p>
            <ul>
              <% for(let x=1; x < Object.keys(profile).length; x++) { %>
              <li><%= Object.keys(profile)[x]%></li>
              <p><%= Object.values(profile)[x]%></p>
              <% }%>
            </ul>
          </div>
          <div class="col-md-3 mx-auto">
            <img src="/assets/image.jpg" class="card-img-top" alt="..." />
          </div>
        </div>
      </div>
      <div class="btn btn-primary">Add to cart</div>
    </div>
  </body>
</html>
